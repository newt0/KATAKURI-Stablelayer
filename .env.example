# KATAKURI env.example (root)
# Copy this to `.env` (or `.env.local`) at the frontend folder as needed.

# Contract package IDs per network (Vite reads these in docs/your-stable-app)
VITE_LOCALNET_CONTRACT_PACKAGE_ID=0xNOTDEFINED
VITE_DEVNET_CONTRACT_PACKAGE_ID=0xNOTDEFINED
VITE_TESTNET_CONTRACT_PACKAGE_ID=0xNOTDEFINED
VITE_MAINNET_CONTRACT_PACKAGE_ID=0xNOTDEFINED

# Optional: Coin type identifiers used by PTB utilities (if implemented in the UI)
# Example formats:
#   0x...::katakuri_usd::KatakuriUSD
#   0x...::usdc::USDC
VITE_KATAKURI_USD_TYPE=
VITE_USDC_TYPE=

# Optional: Market module name (defaults to `market`)
VITE_MARKET_MODULE=market

